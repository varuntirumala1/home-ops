---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-recyclarr-0
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block
